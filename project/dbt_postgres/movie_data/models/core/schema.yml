version: 2

models:
    # - name: dim_genres
    #   description: List of unique movie genres from TMDB identified by genre_id

    - name: fact_movies
      description: All movie data from TMDB, uniquely identified by record_id
      columns:
          - name: record_id
            description: Primary key for this table, a surrogate key generated by Postgres
            tests:
                - unique:
                    severity: warn
                - not_null:
                    severity: warn